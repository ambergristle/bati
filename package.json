{
  "name": "bati",
  "private": true,
  "version": "0.0.605",
  "description": "@batijs monorepo",
  "keywords": [],
  "license": "MIT",
  "author": "Joël Charles <joel.charles91@gmail.com>",
  "type": "module",
  "scripts": {
    "new-boilerplate": "tsx ./scripts/new-boilerplate.ts",
    "screenshot": "node ./scripts/screenshot.js",
    "release": "pnpm -r prerelease && bumpp && pnpm run publish",
    "release:ci": "pnpm -r prerelease && bumpp -y && pnpm run publish --no-git-checks",
    "release:beta": "pnpm -r prerelease && bumpp --preid beta prerelease && pnpm run publish --no-git-checks --tag beta",
    "release:local": "pnpm -r prerelease && bumpp -y --no-push --no-tag --no-commit --preid local prerelease && pnpm run publish --no-git-checks --tag local --registry http://localhost:4873",
    "publish": "vp run build && pnpm -r publish",
    "check-types": "vp run -r check-types",
    "test": "vp run -r build && vp run -r test",
    "test:e2e": "vp run build && pnpm --filter \"@batijs/tests\" test:e2e",
    "test:e2e:workflow-write": "pnpm test:e2e workflow-write",
    "cli": "vp run build && rimraf /tmp/bati-app && node packages/cli/dist/index.js /tmp/bati-app",
    "lint": "biome lint",
    "format": "biome format --write",
    "check": "biome check --write",
    "reset": "git clean -Xdf && pnpm install && vp run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@vikejs/biome-config": "^2.0.1",
    "bumpp": "^11.0.1",
    "citty": "^0.2.1",
    "globals": "^17.4.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vite-plus": "^0.1.14"
  },
  "optionalDependencies": {
    "browserless": "^10.12.5",
    "puppeteer": "^24.40.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.24.0"
}
